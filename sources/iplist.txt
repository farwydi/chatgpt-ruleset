# ChatGPT / OpenAI IP ranges
# One CIDR per line. Bare IPs are accepted and converted to /32 (v4) or /128 (v6).
# Lines starting with `#` and blank lines are ignored.

# IPv4 examples (replace with your own list)
24.199.123.28/32
172.66.0.0/20

# Cloudflare anycast ranges advertised via HTTPS RR ECH ipv4hint for chatgpt.com / openai.com.
# iOS opens QUIC directly to these IPs with encrypted SNI (ECH), so sing-box cannot sniff
# the domain and `domain_suffix` rules do not match. Routing by IP is required.
8.6.112.0/24
8.47.69.0/24

# IPv6 examples
2606:4700::/32
